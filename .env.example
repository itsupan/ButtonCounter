# Copy to .env.development (local dev) and .env.production (production build).
# Both files are gitignored — never commit real credentials.
# Values come from the Turso dashboard: https://turso.tech

TURSO_URL=
TURSO_TOKEN=
