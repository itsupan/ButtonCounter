node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# drizzle-kit scratch output (db:generate, db:pull). Nothing applies it — the
# migrations that do get applied are hand-written in /migrations. See docs/DATABASE.md.
/drizzle

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

*.md
!/docs/*.md
